# Compiled JavaScript files
lib/**/*.js
lib/**/*.js.map

# TypeScript v1 declaration files
typings/

# Node.js dependency directory
node_modules/
*.local

.env

# tesseract.js downloads its language data into the CWD when run locally
# (e.g. via the emulator or a local script) - not something to commit.
*.traineddata