# Template for functions/.env, which is gitignored. Copy this file to
# functions/.env and fill in real values - only used when running under
# the Functions emulator (see functions/src/common.ts getSmtpPassword,
# and process.env.FUNCTIONS_EMULATOR gating throughout functions/src).
# In production these come from Secret Manager instead.

SMTP_PASSWORD=
