import { ClearSelectDirective } from './clear-select.directive';

describe('ClearSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearSelectDirective();
    expect(directive).toBeTruthy();
  });
});
