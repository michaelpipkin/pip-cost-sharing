name: CI
on:
  pull_request_target:
    types: [opened, synchronize]
    branches:
      - release
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR triggered"
      - uses: actions/upload-artifact@v4
        with:
          name: output
          path: /tmp/